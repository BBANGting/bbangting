export type BreadList = {
  breadId: number;
  breadName: string;
  imgUrl: string;
  stock: number;
  maxTingNum: number;
  tingDateTime: string;
  tingStatus: string;
};

export type UserInfo = {
  nickname: string;
  banCount: number;
};